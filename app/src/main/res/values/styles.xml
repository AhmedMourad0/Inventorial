<resources>

    <style name="AppTheme" parent="BaseAppTheme" />

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name = "android:windowActionBar">false</item>
        <item name = "android:windowNoTitle">true</item>
    </style>

    <style name="BaseAppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:fontFamily">sans-serif</item>

        <item name="toolbarStyle">@style/Toolbar</item>

        <item name="cardViewStyle">@style/CardView</item>
        <item name="coordinatorLayoutStyle">@style/Widget.Support.CoordinatorLayout</item>

        <item name="colorControlNormal">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark" />

    <style name="ImagePickerTheme" parent="BaseImagePickerTheme" />

    <style name="BaseImagePickerTheme" parent="ef_BaseTheme">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="toolbarStyle">@style/Toolbar</item>-->
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <style name="Toolbar" parent="Base.ThemeOverlay.AppCompat.ActionBar">
        <item name="titleTextColor">@android:color/black</item>
        <item name="titleTextAppearance">@style/TitleTextAppearance</item>
    </style>

    <style name="ef_CustomToolbarTheme" parent="Toolbar">
        <item name="android:gravity">center_vertical</item>

    </style>

    <style name="TitleTextAppearance" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="DefaultNumberPickerTheme">
        <item name="colorControlNormal">@color/colorAccent</item>
    </style>

</resources>
